1) Compile
   Compile the Java sources

   javac Main.java

2) Run
   Run the program and pass the input file. If you want to specify a start vertex, pass it as the second argument.

   java Main Data.txt 27 

   Note: MY PID(5474827)


or you can directly run the executable by running the command

   java Main Data.txt 27 

   Note: MY PID(5474827)